// let fruits = [ ]
// let fresh_fruits = [apple, originAgentCluster, banana];
// let result = ["Pass", "Fail", "Pass", "Pass", "Fail"];
let mixed_date = [1, "soumya", true, 3.14, false, "null", "deshpande"];
// console.log(mixed_date.length);
console.log(mixed_date[5]);

console.log(mixed_date.at(-1)); // it will give undefined because we cannot access the array element with negative index. It will give undefined if we try to access the array element with index which is greater than the length of the array.