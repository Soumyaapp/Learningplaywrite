let browser = ['chrome', 'firefox', 'edge'];
// console.log(browser.length);
// console.log(browser);
// browser.pop();
// console.log(browser);


for(let i=0; i<browser.length; i++)
{
    console.log(browser[i]);
    if (browser[i] === "firefox")
    {
        console.log("firefox is removed")
    }
}
